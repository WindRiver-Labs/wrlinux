BANNER_FEATURE_TPM12 = "\
The feature/tpm1.2 requires the meta-tpm layer to be enabled.  However, this \
layer has not been included in your project.  You may get warnings or errors \
due to this."

CONFIG_BANNER[feature_tpm12] = "${@bb.utils.contains('BBFILE_COLLECTIONS', 'tpm', '', '${BANNER_FEATURE_TPM12}', d)}"

KERNEL_FEATURES_append += "features/tpm/tpm.scc"

PNWHITELIST_tpm += " \
        openssl-tpm-engine \
        tpm-quote-tools \
        tpm-tools \
        trousers \
        "

